import { Input } from "@mui/material";
import React from "react";

export default function SearchBar(){
    return (
        <div className="mt-20">
            <Input placeholder={"search any for thing "}></Input>
        </div>
    );
}